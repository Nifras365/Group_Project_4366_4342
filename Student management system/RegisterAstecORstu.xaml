<Window x:Class="Student_management_system.RegisterAstecORstu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Student_management_system"
        mc:Ignorable="d"
        Title="RegisterAstecORstu" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Border Background="#E0F2F1">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Width="400">
        <Button Content="Student registration" Margin="0 0 0 0" Style="{StaticResource ButtonST}" Click="Button_Click"/>
        </Border>
        <Border Grid.Column="1" Width="400">
            <Button Content="Teacher registration" Margin="0 0 0 0" Style="{StaticResource ButtonST}" Click="Button_Click_1"/>
        </Border>
    </Grid>
    </Border>
</Window>
